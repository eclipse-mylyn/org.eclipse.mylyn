{ 
  "type": "<%= envtype %>", 
  "url": "/<%= envid %>/", 
  "version": "<%= version %>", 
<% if @envinfo == "" %>
  "info": "<%= envmode %>",
<% else %>
  "info": "<%= envinfo %>/<%= envmode %>",
<% end %>
  "properties":{
    "version": "<%= accessmode %>"
  }
}
